# DS-ReG 研究目标与贡献设计

## 1. 分支来源与本版范围

- 新分支：`codex/ds-reg`。
- 基础分支：`codex/time-aware-relational-consistency`。
- 基础版本：第 1.07.3 版，提交 `e703ae4464a65f0fa032377fbc063964770307de`。
- 第 1.01 版确定问题、三个模块的职责、预期改动和贡献表述。
- 第 1.02 版实现模块二 SDGA、生成器独立系数、P×K 采样、状态匹配对照、分块日志和恢复检查；不改模块一校准公式、模块三重加权公式、原诊断和外部 Word 初稿。

下文将“现有实现”“拟实现设计”“待验证目标”分开。未来贡献稿用于指导研究，不能在实现和证据完成前当作已经取得的实验结论。现有方法说明仍见 [RELATION_CONSISTENCY.md](RELATION_CONSISTENCY.md)，当前可运行命令仍见 [SERVER_RUNBOOK.md](SERVER_RUNBOOK.md)。

第 1.02 版进度：下面模块二提出的分块均值、采样覆盖、状态匹配与独立生成器消融接口已落地；模块一的“系数分离”已完成，类内保真校准仍待研究；模块三的块间调度仍未实现。本地 CPU 测试验证实现正确性，分类收益、方法前后关系改善及论文贡献仍需服务器实验支持。

## 2. 研究主线与参考论文

方法工作名：**Diffusion-State-Aware Relational Geometry（DS-ReG）**，扩散状态感知的关系几何学习。

建议标题：**Learning State-Aware Relational Geometry for Diffusion-Based Zero-Shot Learning**。

中心问题：类别语义承载跨类结构，实例对比表征承载类内结构；仅将两种信息作为生成条件，未必能保证生成特征在不同扩散状态下保持相应关系。研究目标是明确“何种关系、在何种扩散状态下、以何种方式监督”。

这里的“匹配”指两种参考关系分别与相应状态下预测特征的关系结构对齐，并合理组织其监督贡献，不要求类别与实例拓扑相互相等，也不预设两种损失或梯度相等。状态以实际 `alpha_bar[t+1]` / SNR 描述，而不只依赖时间步序号。

### 从两篇论文借鉴什么

本轮参考用户提供的以下 PDF：

1. Ye 等，*ZeroDiff: Solidified Visual-semantic Correlation in Zero-Shot Learning*，ICLR 2025。重点参考正文第 2–4 页的训练/验证已见样本 critic 差异诊断，以及将数据不足、实例语义和多视角判别联系起来的方法组织。
2. Li 等，*ZeroDiff++: Balancing Semantic Diffusion Dynamics for Robust Zero-Shot Learning*。重点参考图 1、§4.1–4.2、表 3–4：先提出 Semantic Diffusion Imbalance，再构建 W/M 互补分支和 SGDF，通过移除、交换、共享分支功能及融合策略对照检查设计。

第二篇是李等的多路径语义扩散工作，与此前讨论中提到的 Ye 等 *ZeroDiff++: Substantial Unseen Visual-semantic Correlation in Zero-shot Learning* 不是同一篇。不要把另一篇的测试时适应/测试时生成归到李等工作中。所提供李等 PDF 首页标注 CVPR Findings，不能仅据文件年份或 CVPR 字样将其等同于 CVPR 主会论文。

借鉴的是问题、设计原则、功能消融之间的对应关系。DS-ReG 研究“关系粒度与扩散状态的适配”，不复制对方的类别复杂度划分或多分支结构。李等表 4 中固定权重融合优于两种门控，只支持其设置下的选择；对本工作而言，它说明第三模块可以以稳健、易验证为优先目标，不必追求复杂门控。

本工作应进一步做方法前后的同口径关系诊断，不能只用分类提升或 t-SNE 图替代“原问题得到缓解”的证据。参考论文的简化理论、可视化和结论也应分别核对，不直接移植为本方法证明。

## 3. 三个模块的目标设想

| 模块 | 名称 | 职责 | 目标证据 |
| --- | --- | --- | --- |
| 一 | Relational Space Calibration（RSC），关系空间校准 | 准备可用且保持双粒度信息的关系表示 | 校准后跨类参考可用，PaCo 类内结构没有因校准而显著丢失 |
| 二 | State-Matched Dual-Granularity Alignment（SDGA），同状态双粒度对齐 | 核心约束：按真实扩散状态分别对齐跨类与类内关系 | 关闭模块三后，相比普通静态/不匹配状态对照仍有收益 |
| 三 | Granularity-Aware State Reweighting（GSR），粒度感知状态重加权 | 在模块二之上组织监督强度，提高训练稳定性和效果 | 相对同一模块二的固定权重版本有可复现的边际收益 |

### 模块一 RSC：准备关系空间，同时保留粒度差异

**现有实现。** 两个投影器在真实 batch 上通过全 batch 距离/角度 RKD 校准；生成器更新时冻结投影器。PaCo 投影器主要由向类别语义对齐的锚定项训练。

**拟改动。**

- 保留真实数据校准、生成器更新时冻结参数的基本机制，避免让生成器与投影器通过共同漂移降低关系损失。
- 重点验证并改善 PaCo 类内结构保真：类别语义在同一类内重复，将其作为全样本锚点可能压缩实例差异。拟将语义锚定限制到跨类结构，并用原始 PaCo 的类内关系作为投影后类内结构的保真参考；不把这一风险表述为已经证实的坍塌。
- 分离校准系数与生成器关系系数。后续“去掉类别/实例生成器约束”消融不应同时改变教师空间。
- 记录校准前后类内距离、秩保真度与投影漂移；不只依据校准 loss 下降认定空间更好。

**期望目标。** 提供兼顾类别结构和实例差异的关系参照。模块一作为核心方法的支撑，不单独承载一条最高层次贡献；新校准策略若没有证据优势，就保留原有校准作为基准。

### 模块二 SDGA：让双粒度关系约束本身具有扩散状态含义

**现有实现。** 类别分组时间步、同时间步掩码、跨类语义/类内 PaCo 分工和按时间步归一化已经实现。第 1.02 版新增显式 SDGA 路径：先计算粒度 × 状态块，再对有效块等权聚合；支持 `16×4` 生成器采样、独立时间步策略和整类混合状态对照。损失约束的是带噪输入下的清洁特征预测 `x_0_fake`，不是直接让两个不同时间步的特征相等。

**拟改动。**

- 将关系目标显式组织成“粒度 × 扩散状态”的损失块 `L_class(t)`、`L_instance(t)`。每块先用自己的有效对归一化、计算均值，随后再进行状态间聚合；模块二固定使用等状态权重即可独立运行。
- 保留语义跨类掩码、PaCo 类内掩码和真实信号保留率标注。同状态匹配和尺度处理属于本模块，不能把全部扩散相关性留给模块三的权重函数。
- 改善有效对覆盖，避免普通随机 batch 在部分状态缺少实例对，或只有两个类别而难以表达丰富的跨类关系。验证类别均衡的关系采样；四个时间步时，`16 类 × 4 样本`、每状态 4 类可作为起始候选，具体采样接入需结合数据支持和开销评估。基线对照必须包含相同采样，不能把采样收益归为对齐收益。
- 增加独立的状态匹配、归一化、生成器类别项和实例项对照。所有对照固定模块一的训练设置；状态匹配消融改变可比较关系对，不能通过打乱生成器自身的正确时间步条件构造过弱对手。
- 用真实扩散状态解释关系监督。可分析独立高斯噪声下样本距离如何受信号/噪声比例影响，作为可比性设计的依据，但不能将带噪输入的距离公式直接当成条件生成器输出性能的证明。

**期望目标。** 即使模块三关闭，SDGA 仍能改善生成特征对两类参考结构的保持，并带来 ZSL/GZSL 收益；状态匹配与粒度分工要有独立证据。模块二是论文核心贡献。

**明确边界。** 本轮目标不要求新建多个扩散分支，也不默认加入跨时间步轨迹一致性。如果未来研究跨状态一致性，需要单独证明关系应保持什么、允许改变什么，不能为了强化“扩散”字样硬加轨迹损失。

### 模块三 GSR：优化监督贡献，不另造梯度冲突主线

**现有实现。** 根据 `alpha_bar[t+1]` 为类别和实例关系设置不同权重，采用有效样本对上的归一化加权平均；没有实时梯度反馈或梯度投影。

**拟改动。**

- 在模块二已计算的 `L_class(t)`、`L_instance(t)` 上施加状态权重，使“块内几何归一化”和“块间监督分配”职责清晰。
- 首选轻量确定性调度，保留固定权重、共享状态调度、粒度差异调度三组对照。现有幂函数、下限和系数只是候选，不预设实例必须衰减更快。
- 将关系项整体强度与时间步分布区分。候选设计以完整扩散日程上的平均权重归一化，使每种粒度的状态权重均值为 1；不再用当前 batch 的加权对数作为状态调度的归一化分母。均匀状态采样/等状态聚合时，可控制其名义总系数；不保证实际损失或梯度范数相等。
- 比较差异调度与共享调度，以及互换类别/实例调度的结果，检验“粒度—状态对应”而非仅检验有没有权重。保留真实状态映射；若检验映射敏感性，只改监督权重映射，不破坏生成器扩散输入。
- 梯度方向与范数作为辅助分析。贡献称“优化关系监督”或“改善训练稳定性”，不称“已解决梯度冲突”。

目标形式为：

```text
w_tilde_k(t) = w_k(s_t) / mean_over_schedule(w_k(s_t))
L_relation = mean_t[lambda_class * w_tilde_class(t) * L_class(t)
                 + lambda_instance * w_tilde_instance(t) * L_instance(t)]
L_generator = L_ZeroDiff + gamma_rel * L_relation
```

这里的状态均值以有效且有覆盖的损失块为前提。实现时须显式处理缺失关系块并记录覆盖率，不能用大量零项冒充相同监督预算；此处为目标形式，不是第 1.07.3 版当前计算公式。

**期望目标。** 以有限额外复杂度改进模块二的训练稳定性和效果。若差异调度不能稳定优于固定/共享调度，不应把它写成已确立的核心规律；第三模块及第三条贡献须据实缩减。

## 4. 诊断如何形成发现问题到验证改善的证据链

诊断应检验研究假设，不以必须出现某条曲线为修改目标。保留原基线结果和计算定义；新增证据与原指标并存，不能静默替换后将新旧数值混用。

### 保留现有完整机制

继续保留冻结基线、类别均衡 episode、跨时间步共享样本/潜变量/噪声、实际信号保留率、checkpoint 检查、CSV 来源记录、关系误差/秩保真度和梯度诊断。当前 `run_baseline` 的干净模型检查不放宽。

### 分三层补充，而不是重写

1. **发现问题。** 在同一 episode 中补真实视觉特征相对语义/PaCo 参考的误差与保真度，报告生成与真实参照的差值。真实视觉不是零误差真值或理论下界。对时间步变化使用 episode 内配对差值，再按训练模型层级统计；现有阴影是标准差，不是置信区间。先 AWA2，再扩展 CUB/SUN，不能根据当前轻微变化预先宣称普遍的显著状态依赖。
2. **验证方法。** 未来另设方法模型评估入口，复用同一指标定义和固定 episode，不把带关系参数的 checkpoint 送进原干净基线入口。主比较在原始视觉空间、固定语义与原始 PaCo 参考下进行；各方法自身投影空间仅作辅助，避免“投影变了”被误当作生成质量改善。仍须结合分类性能评价几何误差的意义。
3. **解释优化。** 在同一模块一/二基础上比较固定、共享、差异调度，辅以有效对覆盖、梯度统计、训练波动和开销。梯度分析为解释服务，不替代主关系指标和分类结果。

受控诊断目前使用真实 PaCo 条件，编码器存在时潜变量也依赖真实视觉。需明确这是受控的条件预测诊断；未见类别的自由生成效果由标准分类评估及后续独立生成分析支撑。不能将受控预测直接称为真实未见生成轨迹。

## 5. 三点贡献的目标稿

以下措辞对应未来要完成的研究，当前尚未全部实现或验证。正文提交前必须根据实际实验收紧结论，不提前加入“首次”“显著”“SOTA”或理论保证。

1. **发现并刻画问题。** 我们研究扩散式零样本学习中的双粒度关系保持与扩散状态之间的适配问题，构建以真实特征关系为参照的受控逐状态诊断，分别考察生成特征对跨类语义结构和类内实例结构的保持程度及其状态响应，为状态感知的关系监督提供依据。
2. **提出核心方法。** 我们提出扩散状态感知的关系几何学习框架 DS-ReG，以保留双粒度信息的关系空间校准为基础，构建同状态双粒度关系对齐方法，在可比较的扩散状态下分别对齐类别语义与实例对比关系，并通过独立的关系尺度处理与有效对组织，将 ZSL 的结构知识融入扩散特征生成。
3. **提出优化方法。** 我们设计粒度感知的扩散状态重加权方法，在双粒度关系对齐的基础上，以实际信号保留率组织两类监督的时间步贡献，分离关系整体强度与状态分配，为关系几何学习提供轻量、可控的优化机制，目标是进一步改善训练稳定性与未见类别识别。

贡献一的“发现”最终应具体到数据支持的现象；若状态响应差异不稳定，则保留关系保持缺口，不宣称普遍的粒度—状态失配规律。贡献二的校准、采样和对齐应通过独立对照分清收益。贡献三的稳定性与识别提升必须有相对于模块二的增量实验证据。

## 6. 分阶段目标与判断标准

| 阶段 | 工作重点 | 完成标准 |
| --- | --- | --- |
| 第 1.01 版，已完成 | 建分支、整理目标和贡献 | 基于指定提交；只有设计文档变更；算法与诊断保持原样 |
| 第 1.02 版，当前 | 模块二实现与单种子验收接口 | CPU 回归、真实训练步骤及恢复测试通过；分类结果待服务器验收 |
| 后续阶段 | 诊断参照与模块一可行性 | 原诊断可复现；新增真实参照和校准保真检查有明确含义 |
| 核心阶段 | 模块二及独立消融 | 模块三关闭时仍有关系与分类收益；区别于仅换采样或普通静态 RKD |
| 优化阶段 | 模块三简洁调度 | 固定/共享/差异调度公平比较，判断是否值得保留独立第三贡献 |
| 论文阶段 | 多数据集、多训练种子、方法前后机制证据 | 初步结果能重复，公平协议明确，结论与证据一一对应 |

近期先运行手册第 0 节的 AWA2 三组实验：保留校准但关闭 G 关系约束、SDGA 同状态、SDGA 混合状态。原 M0/M2/M5 仍可作为历史路径参照。固定 DRG 比较 DFG，记录采样、训练长度、语义嵌入、合成数量、评估视角和模型选择协议。不同论文表格中的分数在配置未核对前不直接横比。

模块二的成功不以模块三必须获胜为前提；整体论文主线以可验证的粒度与状态适配为中心。目标是形成有机制解释且可复现的改进，具体提升幅度和投稿成熟度由后续证据决定。
